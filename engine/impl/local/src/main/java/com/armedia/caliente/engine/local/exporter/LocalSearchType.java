package com.armedia.caliente.engine.local.exporter;

public enum LocalSearchType {

	//
	PATH, // Recursive directory search, like before
	LIST, // A file containing paths within
	SQL, // A SQL Query
	//
	;

}